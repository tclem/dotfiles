Look at PRs I've recently merged (in the last 24 hrs) in github/github-app and create a little line to copy paste into slack with the format:

`:merged: [PR_TITLE](PR_URL)`

If there are no new PRs since the last run: say so instead of repeating them. I'll ask to run again periodically, use the timestamp of the last run to know how far back we need to check to get relevant data.
