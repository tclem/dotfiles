# Clipboard (macOS + tmux)
set -g set-clipboard on
set -s copy-command "pbcopy"

# Focus events for editors/tools that care
set -g focus-events on

# True color support
set -g default-terminal "tmux-256color"
set -as terminal-overrides ",xterm*:Tc"
